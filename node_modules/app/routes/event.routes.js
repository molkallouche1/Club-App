module.exports = (app) => {
    const events = require('../controllers/event.controller.js');

    // Create a new event
    app.post('/event', notes.create);

    // Retrieve all events
    app.get('/events', eventss.findAll);

    // Retrieve a single event with eventId
    app.get('/events/:eventId', events.findOne);

    // Update a event with eventId
    app.put('/events/:eventId', nevents.update);

    // Delete an event with eventId
    app.delete('/events/:eventId', event.delete);
}